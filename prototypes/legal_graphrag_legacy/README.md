# Legal GraphRAG Legacy Prototype

This is an archived prototype. **Do not import from** `apps/`, `src/pipeline`, `src/retrieval`,
`src/generation`, `src/infrastructure`, or `src/shared`.

Use for reference only. Active implementation is in `src/` and `apps/`.
