Viewed docker-compose.yml:17-41

State tiếp theo của chúng ta chính là **Gate 7 (Expand to the Curated Corpus)** và sau đó là chốt **Milestone A** để chuyển sang **Phase 2 (Truy xuất GraphRAG)**.

Cụ thể lộ trình những bước cuối cùng của Milestone này như sau:

1. **Bổ sung Data (Missing Readiness Work):**
   - Bắt buộc phải crawl 3 văn bản còn thiếu (`ND01_2021`, `ND47_2021`, `TT01_2021`) về thư mục `data/raw/`. (Milestone A quy định không được lấy văn bản khác để thay thế).

2. **Chạy Full Pipeline cho 3 văn bản này:**
   - Lặp lại quy trình vừa thành công ở `L59_2020` cho cả 3 văn bản mới: `Parse` -> `Extract` (chạy qua Gemini API với cấu hình Batch 2) -> `Validate Payload` -> `Write` -> `Embed` -> `Graph Quality`.

3. **Tổng hợp Milestone A Evidence (Nghiệm thu):**
   - Lưu trữ kết quả test query (vector sanity check).
   - Xuất các file báo cáo tổng hợp vào `results/milestone_a/`.
   - Tick hoàn thành 12 tiêu chí của Acceptance Matrix và cập nhật trạng thái Timeline.

4. **Handoff to Phase 2 (Chuyển sang Giai đoạn 2):**
   - Khi Milestone A đã chốt, Neo4j của chúng ta đã có một Data Graph "xịn" (chuẩn cấu trúc, có nhúng Vector). Chúng ta sẽ bắt tay vào làm tính năng cốt lõi nhất: **GraphRAG Retrieval** (Tìm kiếm kết hợp Vector + Đi bộ trên Đồ thị để lấy ngữ cảnh).

---
**Để có thể bước vào State tiếp theo này, mình vẫn đang chờ bạn:**
Gửi mình 3 cái URL của `ND01_2021`, `ND47_2021`, `TT01_2021` trên `vbpl.vn` để mình gõ lệnh crawl lấy data về nhé!


[/] Step 0: Precondition check (Milestone A passed)
[ ] Lấy 3 URL (ND01_2021, ND47_2021, TT01_2021)
[ ] Chạy lệnh crawl và xử lý pipeline cho 3 văn bản này
[ ] Gen nghiệm thu Milestone A